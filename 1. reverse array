#include<stdio.h>

int main() {

    int size=5,i;
    float arr[size];

    printf ("enter the array: ");
    for (i=0 ; i<5 ; i++)
        scanf ("%f", &arr[i]);

    printf ("the array in reversed order: ");
    for (i=size-1 ; i>=0 ; i--)
        printf ("%.2f\t",arr[i]);

    return 0;
}
